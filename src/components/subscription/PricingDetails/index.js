import { useCallback, useState, forwardRef } from "react";

function PricingDetails() {
  return (
    <div className={`grow-0 shrink-0 basis-auto px-[21px]`}>
      <p className={`[font-family:Poppins] text-[25px] font-medium text-[rgba(224,224,224,1)]`}>We offer both monthly and yearly plans with a 15 day trial period</p>
    </div>
  );
}

export default PricingDetails;
